export const fonts = {
  // Elegant serif font with character for headings
  heading: '"Playfair Display", serif',
  
  // Clean, readable sans-serif for body text
  body: '"Source Sans Pro", sans-serif',
  
  // For rustic charm in special elements
  accent: '"Bitter", serif',
  
  // Monospace font for code blocks
  mono: 'SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace',
};

// Font weights
export const fontWeights = {
  normal: 400,
  medium: 500,
  semibold: 600,
  bold: 700,
  extrabold: 800,
};

// Line heights
export const lineHeights = {
  normal: 'normal',
  none: 1,
  shorter: 1.25,
  short: 1.375,
  base: 1.5,
  tall: 1.625,
  taller: 2,
};

// Letter spacings
export const letterSpacings = {
  tighter: '-0.05em',
  tight: '-0.025em',
  normal: '0',
  wide: '0.025em',
  wider: '0.05em',
  widest: '0.1em',
};